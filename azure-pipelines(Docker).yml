# C/C++ with GCC
# Build your C/C++ project with GCC using make.
# Add steps that publish test results, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/apps/c-cpp/gcc

trigger:
- main

pool: Default

steps:
- script: |
    /home/ldra-runner/ldra_toolsuite/contestbed /home/ldra-runner/ldra_workarea/examples/toolsuite/Cashregister_7.0/Cashregister.tcf /112a302120312panq
  displayName: 'Run a multi-line script'

- task: PublishPipelineArtifact@1
  inputs:
    targetPath: '/home/ldra-runner/ldra_workarea/Cashregister_7_tbwrkfls/Cashregister_7_reports/'
    artifact: 'zip'
    publishLocation: 'pipeline'